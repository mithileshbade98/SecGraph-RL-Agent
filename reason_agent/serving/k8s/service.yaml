apiVersion: v1
kind: Service
metadata:
  name: secgraph-rl-agent
  labels:
    app: secgraph-rl-agent
spec:
  type: LoadBalancer  # Change to ClusterIP for internal-only
  selector:
    app: secgraph-rl-agent
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
    name: http
